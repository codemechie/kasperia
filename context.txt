=== SESSION CONTEXT: 21 May 2026 ===
=== SENIOR DEV: opencode (big-pickle) ===
=== JUNIOR DEV: saadatwani ===

GOAL:
AI Agent that searches 10-50 e-commerce sites for best deals. No LLM training — pure RAG + tool calling.
Ollama (Qwen/Mistral) orchestrates via MCP tool server.

---

ARCHITECTURAL OVERVIEW (Abstract Interface / Agent-Driven Scraping)

The project underwent a major re-architecting. The old approach (direct-scraping Nike/Adidas with
ad-hoc sync functions) has been replaced with an abstract, agent-driven pipeline:

┌──────────────────────────────────────────────────────────────────┐
│                        main.py                                  │
│  Entrypoint — bootstraps FastMCP server with graceful lifecycle │
│  (logging, KeyboardInterrupt, exception handling, finally).     │
└──────────────────────────┬───────────────────────────────────────┘
                           │ mcp.run()
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│                       server.py                                 │
│  FastMCP "Kasperia Deal Agent". Tools:                          │
│    • search_products(query, domain) — 3-layer lookup:           │
│        1. state.cache hit → instant return                      │
│        2. state.scraper_registry hit → async scrape_products()  │
│        3. Cache+registry miss → push to state.queue +           │
│           run synchronous fallback parser (JSON-LD / OG tags)   │
│    • load_background_services (intended to start the worker)    │
│  Lifecycle hooks at module level (if __name__ == "__main__").   │
└──────┬──────────────────────┬───────────────────────┬───────────┘
       │                      │                       │
       ▼                      ▼                       ▼
┌──────────────┐    ┌─────────────────┐    ┌────────────────────┐
│  state.py    │    │   worker.py     │    │  scrapers/         │
│  AppState    │    │  Background     │    │  └ base.py         │
│  Container   │    │  Async Worker   │    │     BaseScraper    │
│              │    │                 │    │     (ABC)          │
│ • registry   │    │ Listens on      │    │     ↑     ↑       │
│ • cache      │◄───│ state.queue     │    │     │     │       │
│ • queue      │    │ for missing     │    │  nike.py adidas.py│
│ • lock       │    │ domain events   │    │  (not yet adapted)│
└──────────────┘    └─────────────────┘    └────────────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │  agent.py    │
                    │ (Ollama)     │
                    │              │
                    │ Qwen calls   │
                    │ search_...   │
                    └──────────────┘

LAYERED DATA FLOW:
  User Prompt → Ollama (tool_choice="auto") → search_products(query, domain)
    → Layer 1: state.cache (instant return)
    → Layer 2: state.scraper_registry (async BaseScraper.scrape_products)
    → Layer 3: Queue agent build + fallback parser (JSON-LD / OG meta)

---

NEW / REFACTORED FILES

┌─────────────────────────────────────────────────────────────────────┐
│ scrapers/base.py       ABSTRACT INTERFACE                          │
│                         • BaseScraper(ABC)                          │
│                         • target_domain: str property               │
│                         • async scrape_products(query) -> List[dict]│
├─────────────────────────────────────────────────────────────────────┤
│ scrapers/schema.py     PYDANTIC SCHEMA                             │
│                         • ProductDeal(BaseModel)                    │
│                         • fields: title, price, currency,           │
│                           product_url, image_url, store_name,       │
│                           scraped_at                                │
├─────────────────────────────────────────────────────────────────────┤
│ state.py               STATE CONTAINER (mock for Redis)            │
│                         • AppState:                                 │
│                           - scraper_registry: Dict[str, Scraper]    │
│                           - cache: Dict[str, Any]                   │
│                           - queue: asyncio.Queue                    │
│                           - lock: asyncio.Lock                      │
├─────────────────────────────────────────────────────────────────────┤
│ worker.py              BACKGROUND ASYNC WORKER                     │
│                         • start_background_worker():                │
│                           infinite loop consuming state.queue       │
│                           placeholder for dynamic scraper agent     │
├─────────────────────────────────────────────────────────────────────┤
│ server.py              FAST MCP SERVER                             │
│                         • FastMCP("Kasperia Deal Agent")            │
│                         • async search_products(query, domain)      │
│                         • 3-layer lookup + fallback parser          │
│                         • extract_product_data() (sync)             │
│                         • @mcp.tools() load_background_services     │
├─────────────────────────────────────────────────────────────────────┤
│ main.py                GRACEFUL ENTRYPOINT                         │
│                         • logging setup                             │
│                         • try/except/finally lifecycle              │
│                         • calls mcp.run()                           │
├─────────────────────────────────────────────────────────────────────┤
│ agent.py               OLLAMA CLIENT (stale — see bugs)            │
│                         • OpenAI-compatible → Ollama :11435         │
│                         • tool schema for search_products           │
│                         • fallback _parse_tool_call regex parser    │
└─────────────────────────────────────────────────────────────────────┘

EXISTING CODE (not yet adapted to new architecture):
  scrapers/nike.py    — NikeScraper with sync .search(), no BaseScraper inheritance
  scrapers/adidas.py  — AdidasScraper with sync .search(), no BaseScraper inheritance
  scrapers/__init__.py — SCRAPERS dict with instantiated old-interface objects

---

BUGS / ISSUES (CRITICAL — must fix)

┌─────┬────────────────────────────────────────────────────────────────────────────────────────┬────────────┐
│ ID  │ Description                                                                           │ Severity   │
├─────┼────────────────────────────────────────────────────────────────────────────────────────┼────────────┤
│ B1  │ server.py:125 — @mcp.tools() is NOT a valid FastMCP decorator.                         │ CRITICAL   │
│     │ Should be @mcp.tool(). The function load_background_services() is never registered,    │            │
│     │ so start_background_worker() is NEVER CALLED. The entire async worker system is dead.  │            │
│     │ FastMCP only supports: @mcp.tool(), @mcp.resource(), @mcp.prompt().                    │            │
├─────┼────────────────────────────────────────────────────────────────────────────────────────┼────────────┤
│ B2  │ state.py:9 — self.lock is declared as type annotation but NEVER INITIALIZED:            │ CRITICAL   │
│     │   self.lock: asyncio.Lock   ← no assignment                                           │            │
│     │ Any code accessing state.lock will raise AttributeError.                               │            │
│     │ Fix: self.lock = asyncio.Lock()                                                        │            │
├─────┼────────────────────────────────────────────────────────────────────────────────────────┼────────────┤
│ B3  │ agent.py ↔ server.py — FUNCTION SIGNATURE MISMATCH.                                   │ CRITICAL   │
│     │ agent.py calls: search_products(prompt=..., brands=..., min_price=..., max_price=...)  │            │
│     │ server.py defines: search_products(query: str, domain: str) -> str                    │            │
│     │ This will raise TypeError: unexpected keyword arguments. The agent talks to the old    │            │
│     │ interface. The server implements the new interface. They are completely incompatible.  │            │
├─────┼────────────────────────────────────────────────────────────────────────────────────────┼────────────┤
│ B4  │ server.py:96 — VARIABLE NAME BUG: uses `domain` instead of `domain_clean`:             │ HIGH       │
│     │   if domain in state.scraper_registry:                                                 │            │
│     │ Should be `domain_clean` (the sanitized lowercase version from line 84).               │            │
│     │ With raw `domain`, lookup may fail due to whitespace/case mismatches.                  │            │
├─────┼────────────────────────────────────────────────────────────────────────────────────────┼────────────┤
│ B5  │ nike.py, adidas.py — DO NOT IMPLEMENT BaseScraper.                                     │ HIGH       │
│     │ They use sync .search(query) instead of async .scrape_products(query).                 │            │
│     │ They lack target_domain property. They are not compatible with the new architecture.   │            │
│     │ Even if registered in state.scraper_registry, server.py line 100 would fail:           │            │
│     │   await scraper.scrape_products(query)  ← sync method, not awaitable                  │            │
├─────┼────────────────────────────────────────────────────────────────────────────────────────┼────────────┤
│ B6  │ worker.py — PLACEHOLDER ONLY. Receives queue events, sleeps 5s, logs "success",        │ HIGH       │
│     │ but NEVER actually builds or registers any scraper. The scraper_registry stays empty.  │            │
│     │ Combined with B1, this whole subsystem is dead code.                                   │            │
├─────┼────────────────────────────────────────────────────────────────────────────────────────┼────────────┤
│ B7  │ scrapers/__init__.py — OLD INTERFACE. Exports pre-instantiated NikeScraper() and       │ MEDIUM     │
│     │ AdidasScraper() with sync .search(). Not consumed anywhere in the new architecture.     │            │
│     │ server.py uses state.scraper_registry, not scrapers.SCRAPERS. Dead code.               │            │
├─────┼────────────────────────────────────────────────────────────────────────────────────────┼────────────┤
│ B8  │ requirements.txt — MISSING DEPENDENCIES. Neither pydantic (schema.py) nor              │ MEDIUM     │
│     │ openai (agent.py) are listed. Install will succeed but imports at runtime will fail.   │            │
├─────┼────────────────────────────────────────────────────────────────────────────────────────┼────────────┤
│ B9  │ server.py:22 — USD_TO_INR = 83 is defined but NEVER USED in the new search_products.   │ LOW        │
│     │ Leftover from old architecture. Live currency conversion was removed.                  │            │
├─────┼────────────────────────────────────────────────────────────────────────────────────────┼────────────┤
│ B10 │ server.py:121 — TYPO: "backgorund" should be "background" (in the status message).     │ LOW        │
└─────┴────────────────────────────────────────────────────────────────────────────────────────┴────────────┘

NEXT STEPS:
  1. Fix B1: change @mcp.tools() → @mcp.tool() on load_background_services
  2. Fix B2: initialize self.lock = asyncio.Lock() in AppState.__init__
  3. Fix B3: reconcile agent.py's tool definition + call signature with server.py's actual signature
  4. Fix B4: domain → domain_clean on line 96
  5. Adapt NikeScraper + AdidasScraper to inherit from BaseScraper (async scrape_products + target_domain)
  6. Implement actual scraper generation in worker.py (not just sleep + log)
  7. Add pydantic + openai to requirements.txt
  8. Wire up __init__.py to register adapters into state.scraper_registry at boot
